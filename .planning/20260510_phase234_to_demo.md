## 任务：Phase 2 + 3 + 4 — 直推到第一个可验证 demo

**背景**：Phase 1 (`d52c49f`) 已封档，paperterm 当前仅有 `paperterm version` 子命令。本次目标是让 `paperterm check <paper_dir>` 真正能跑出 lint 报告，即用户能用 raw2event paper 验证的端到端 demo。中途 Phase 2 (schema)、Phase 3 (LaTeX walker) 都没有可独立 demo 的产出，所以打包推进。

**用户授权**：用户明确「只要 codex 严格审查通过，我就不再检查」+「完成第一个可验证 demo 时停下来」。每 Phase 完成后送 codex 审，通过即 commit + push，连续走完 Phase 2-4 后停下做 demo。

**Phase 2-4 deliverable 总览**（plan §12 既有规划，本文档不重复）：

| Phase | deliverable | commit |
|---|---|---|
| Phase 2 | `src/paperterm/glossary.py` (pydantic Concept/Form/Glossary + extends 含循环检测) + `src/paperterm/data/base/{ml-common,event-camera}.yaml` + `tests/test_glossary.py` + `tests/test_extends.py` | 单一 `feat(glossary): yaml schema with extends` |
| Phase 3 | `src/paperterm/latex.py` (pylatexenc AST + context-aware text iterator) + `tests/test_latex.py` + `tests/fixtures/tex/*.tex` | 单一 `feat(latex): context-aware AST walker` |
| Phase 4 | `src/paperterm/linter.py` (rule matcher) + `src/paperterm/report.py` (line 格式输出) + 在 `cli.py` 加 `check` 子命令 + `tests/test_linter.py` + `tests/fixtures/glossary/*.yaml` | 单一 `feat(linter): rule matcher + check subcommand` |

**关键决策（不另行确认，遵守 plan）**：
1. **schema 严格遵守 plan §5**：dogfood findings 的 `related_to` (S1) / `first_mention` (S2) **不**在本期加入。它们是「nice-to-have」字段，Phase 5 dogfood revisit 时再决定，避免 schema 现在凝固未充分验证的字段
2. **Phase 2 base yaml 内容**：`ml-common.yaml` ~5 个通用 ML 概念（accuracy / F1 / mAP / SGD / Adam），`event-camera.yaml` ~5 个事件相机术语；不能用 raw2event 论文的具体术语（避免 example/output/glossary.draft.yaml 与 base 互相耦合）
3. **Phase 3 LaTeX context**：实现 plan §3.4 列出的 8 种 context；`prose/caption` 视为可 lint，`math/verbatim/comment/cite_arg` 视为 skip；`table/figure` 暂时归入 prose（Phase 4 可能要再细分以支持 `contexts:` field 限定，本期保留接口但不细分）
4. **Phase 4 报告格式**：先实现 `line` 格式（人眼可读），`json/sarif` 留 Phase 7；`paperterm check` 退出码遵照 plan §6.3：0 = 无 violation，1 = 有 violation，2 = 程序错误
5. **依赖增量加**：Phase 2 加 `pydantic>=2.5` + `pyyaml>=6.0`；Phase 3 加 `pylatexenc>=2.10`；Phase 4 加 `rich>=13.0`（终端着色）。每 Phase commit 同步更新 `pyproject.toml`

**质量门禁**（每 Phase 强制满足）：
- `pytest tests/` 全绿，新功能必须配测试（plan §12 实施流程第 2 步）
- `ruff check .` + `ruff format --check .` 全绿
- `mypy` 全绿（strict mode 已开）
- 单 commit 不带 broken intermediate state（一个 Phase 的所有源码 + 测试 + pyproject 改动一起入库）

**Per-Phase 工作流**：

```
1. 实施代码 + 测试
2. 本地跑 pytest + ruff + mypy 全绿
3. codex 短审产物（"写了 X、Y、Z；ruff/mypy/pytest 已绿；看 design 是否合理"）
4. 修订 → 再过 codex
5. 单 commit + push
6. 进下一 Phase
```

**Demo 验收（Phase 4 commit 之后）**：
- plan §5.2 规定 `paperterm check` 见到 `canonical: TBD` 或 `found_forms` 会**拒绝运行**（draft yaml 不能直接喂）。所以 demo 前需手工转换：
  - 从 `example/output/glossary.draft.yaml` 挑 5–10 个 confidence ≥ 0.9 且 forms ≥ 3 的 concept（典型如 `per_pair_k_calibration` 10 forms、`metric_px_emd` 6 forms、`upstream_diagnostic` 6 forms）
  - 手工转成 `example/raw2event_glossary.yaml`：删 `found_forms` / 选定 `canonical` / 其他 forms 设为 `aliases` / 加 `version: 1`
  - 这个手工转换本身就是 paperterm 真实使用流程的一部分（dogfood findings.md §6 P2 也指向这里）
- 用 `example/raw2event_glossary.yaml` 在 `/vol1/1007/projects/raw2event/doc/paper/` 上跑 `.venv/bin/paperterm check`
- 产物 log 留 `example/dogfood_check_output.txt`（commit 入库）
- 写一段简短报告（追加到 `example/findings.md` 末尾或新建 `example/demo.md`）说明 demo 怎么跑、看哪里
- **暂停**让用户验证

## 待确认事项

无（plan §12 已经足够具体；dogfood findings 不在本期 schema 增强）。

## 修订记录

- 2026-05-10 codex 一审：(1) Phase 4 deliverable 表格里写了 line/JSON 与决策 line-only 冲突 → 改为 line 格式；(2) Demo 用 dogfood draft yaml 直接喂 check 会被拒（plan §5.2）→ 改为「先手工把 draft 转成正式 glossary」
- 2026-05-10 user 反馈：sub-phase 也要 commit → Phase 2 拆 P2.A/B/C 三个 sub-stage commit；Phase 3/4 deliverable 内部高度耦合，仍单 commit/Phase

## 完成记录

| Phase | sub-stage | commit |
|---|---|---|
| Phase 2 | P2.A pyproject deps + glossary.py（schema + extends + paperterm:base/ URI） | `4f4b665` |
| Phase 2 | P2.B base yaml (ml-common + event-camera) | `079b983` |
| Phase 2 | P2.C tests/test_glossary.py + tests/test_extends.py（25 tests） | `be8bf6c` |
| Phase 3 | latex.py walker + 6 fixtures + tests/test_latex.py（16 tests，含 dogfood findings P2 回归） | `8865eca` |
| Phase 4 | linter.py + report.py + cli.check + tests/test_linter.py（14 tests，含 CLI exit code 0/1/2） | `25c5b6b` |
| Demo | example/raw2event_glossary.yaml + dogfood_check_output.txt + demo.md（18 violations on raw2event） | `d006739` |

**全程门禁**：57 tests pass / ruff check + format / mypy strict 全绿，每 commit 通过 codex 短审。

**第一个可验证 demo**：`paperterm check /vol1/1007/projects/raw2event/doc/paper --glossary example/raw2event_glossary.yaml --include 'sections/**/*.tex' --include 'tables/*.tex'` → 18 violations / exit 1，覆盖 plan §1.1 两条 motivating example。

**用户验收点**：暂停在此处。下一步进 Phase 5 (`paperterm bootstrap` + `paperterm print-prompt`) 需要用户许可。
